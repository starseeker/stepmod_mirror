------------------------------------------------------------
MIM LONG FORM
------------------------------------------------------------
The SHTOLO algorithm leaves an empty select:

TYPE characterized_definition = SELECT 
  ();
END_TYPE;

so replace with

TYPE characterized_definition = SELECT 
  (characterized_product_definition);
END_TYPE;

TYPE characterized_product_definition = SELECT
   (product_definition);
END_TYPE; 

EEP does not deal with function with in functions
So I hand edited:
FUNCTION enclose_pregion_in_pregion
FUNCTION equal_maths_values
FUNCTION simplify_function_application
FUNCTION simplify_generic_expression
FUNCTION stripped_typeof

Also the function:
dependently_instantiated
and the rule: validate_dependently_instantiable_entity_data_types
are not valid express edition 1, so removed.

Also SHTOLO incorrectly adds:
CONSTANT
    base_types

Change sql_mappable_defined_function to SQL_mappable_defined_function