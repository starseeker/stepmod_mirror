<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="../../../xsl/express.xsl"?>
<!DOCTYPE express SYSTEM "../../../dtd/express.dtd">

<express language_version="2" rcs.date="2005-11-22T12:07:23" rcs.revision="1.0" description.file="arm_descriptions.xml">
<application name="JSDAI" owner="LKSoft" url="www.lksoft.com" version="4.0 beta" source="physical_unit_non_planar_design_view_arm schema_instance"/>
<schema name="Physical_unit_non_planar_design_view_arm">
<interface kind="use" schema="Manifold_subsurface_arm"/>
<interface kind="use" schema="Physical_unit_design_view_arm"/>
<entity name="Assembly_component_non_planar_shape" supertypes="Manifold_subsurface_model">
<explicit name="shape_characterized_component">
<aggregate type="SET" lower="1" upper="?"/>
<typename name="Assembly_component"/>
</explicit>
<where label="WR1" expression="NOT EXISTS(SELF\Representation.description)"/>
</entity>
<rule name="unique_assembly_component_non_planar_shape" appliesto="Assembly_component_non_planar_shape">
<algorithm>LOCAL
     ac : BAG OF Assembly_component := [];
   END_LOCAL;
   REPEAT i := 1 to SIZEOF(Assembly_component_non_planar_shape) by 1;
     ac  := ac + Assembly_component_non_planar_shape[i].shape_characterized_component;
   END_REPEAT;</algorithm>
<where label="WR1" expression="SIZEOF(ac) = SIZEOF(bag_to_set(ac))"/>
</rule>
<function name="bag_to_set">
<parameter name="the_bag">
<aggregate type="BAG" lower="0" upper="?"/>
<builtintype type="GENERIC"/>
</parameter>
<aggregate type="SET" lower="0" upper="?"/>
<builtintype type="GENERIC"/>
<algorithm>LOCAL
      the_set : SET OF GENERIC : intype := [];
      i       : INTEGER;
    END_LOCAL;

    IF SIZEOF(the_bag) &gt; 0 THEN
      REPEAT i := 1 TO HIINDEX(the_bag);
        the_set := the_set + the_bag[i];
      END_REPEAT;
    END_IF;
    RETURN(the_set);</algorithm>
</function>
</schema>
</express>
