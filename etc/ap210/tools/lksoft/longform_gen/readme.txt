1) Configure tool's properties in file longform.props .
2) Change the output file name in longform.bat (the default is longform.exp)
3) Run longform.bat

--tt modified to make individual arm,mim batch files.

test only of using url in comment field.